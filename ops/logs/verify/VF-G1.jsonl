{"at":"2026-09-21T21:31:27.895Z","lane":"verify","kind":"lane","ref":"verify/VF-G1","status":"ok","durationMs":0,"costUsd":0,"note":"Lượt crux-worker-2 nhận mục verify/VF-G1 ở phụ lục P1 bước 3. BƯỚC 0 (P3): đo hai phép trên cả 12 PR đang mở — phép A (có merge=union) và phép B (mô phỏng GitHub theo KF-009) đều EXIT=0 cho cả 12, hàng đợi xung đột RỖNG, không giải gì. BƯỚC 2: gọi pickPrToHandle của ops/scripts/pr-triage.ts với dữ liệu thật (check-runs, comment, giờ commit cuối của 12 PR) — trả null, không PR nào cần nhận. KẾT QUẢ MỤC: tách G1 thành hai vế. Vế (b) hệ quả vận hành ĐÃ KIỂM bằng chạy thật từ ops/logs: cấu hình 3 worker đang chạy, dự phòng Plan B 2 worker chưa phải dùng tới. Số liệu đầy đủ ở docs/assumptions.md mục G1 và ở phụ lục P1 — không chép lại vào đây, vì một dòng log kể lại số lượt của routine khác chính là lỗi mà lượt này phải sửa. Vế (a) \"tài khoản có tính năng Projects\" KHÔNG tự kiểm được (trang cấu hình tài khoản), vẫn chờ chủ dự án ở issue #5 — nên mục giữ status review, không done. Giới hạn khai trước: log đếm THIẾU vì lượt idle không commit gì, nên mọi con số là cận dưới; chốt được \"ít nhất ba worker\", không chốt được nhịp chính xác của từng worker. Đã thêm bài kiểm hồi quy worker-fleet-cadence vào pnpm recheck:assumptions, 11 test gồm test âm cho cả bốn chiều hỏng; bốn phép phá thật đều đỏ đúng. Hai lỗi của chính lượt này, tìm ra bằng phép phá và bằng đối chiếu số với máy, đã sửa trong PR: một test tự fail-open, và phép đếm thổi số vì đếm mọi tên routine nhắc trong note. pnpm check EXIT=0 432/432 pass 0 fail 0 skipped; pnpm replay khớp snapshot 6/6. costUsd: 0 (không gọi API trả tiền)."}
