{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "crux://contracts/title-formulas.schema.json",
  "title": "Danh sách khuôn tiêu đề của một kênh",
  "description": "KHÔNG có trong spec gốc — thêm ở mục release/R-001 để kernel đối chiếu titles[].formula của xưởng release bằng máy, thay vì chấp nhận mọi chuỗi. Đóng (additionalProperties: false), trừ $note.",
  "type": "object",
  "required": [
    "channel",
    "formulas"
  ],
  "additionalProperties": false,
  "properties": {
    "$note": {
      "type": "string"
    },
    "channel": {
      "type": "string",
      "minLength": 1
    },
    "formulas": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": [
          "id",
          "name"
        ],
        "additionalProperties": false,
        "properties": {
          "id": {
            "type": "string",
            "pattern": "^[a-z][a-z0-9-]*$",
            "description": "Khoá dữ liệu tiếng Anh (CLAUDE.md mục 9), đối chiếu với titles[].formula của payload xưởng release."
          },
          "name": {
            "type": "string",
            "minLength": 1,
            "description": "Tên khuôn tiếng Việt, đúng cột 'Khuôn' của title-formulas.md."
          }
        }
      }
    }
  }
}
